# User Service Configuration (Symfony)
# This service runs independently but can be accessed through the gateway

spring.application.name=user-service
server.port=8000

# Service URL for gateway routing
service.url=http://localhost:8000

# MongoDB Configuration (managed by Symfony)
# mongodb.url=mongodb://localhost:27017
# mongodb.database=elearning_users

# Keycloak Configuration (managed by Symfony)
# keycloak.url=http://localhost:8081
# keycloak.realm=elearning
